import { DynamicOverlayDirective } from './dynamic-overlay.directive';

describe('DynamicOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
